/** Term/definition pairs for the flashcards study tool. */
export const FLASHCARDS = [
  { front: 'Route', back: 'One integration flow: exactly one from() (the consumer) followed by a chain of processing steps.' },
  { front: 'Endpoint', back: 'A concrete message source or destination, addressed by URI: scheme:contextPath?options — e.g. kafka:orders?groupId=svc.' },
  { front: 'Exchange', back: 'The envelope that travels a route: carries the Message (body + headers), exchange properties, and any exception.' },
  { front: 'CamelContext', back: 'The runtime container holding all routes, components and registries. On Quarkus it is created and started for you.' },
  { front: 'Processor', back: 'Any step that works on the exchange — your own lambda/bean or a built-in EIP.' },
  { front: 'EIP', back: 'Enterprise Integration Pattern — a named, reusable solution to a recurring messaging problem (Hohpe & Woolf, 2003). Camel is the reference implementation.' },
  { front: 'Content-Based Router', back: 'Routes each message to a different destination based on its content. Camel: choice() / when() / otherwise().' },
  { front: 'Message Filter', back: 'A one-branch router: matching messages continue, the rest are silently dropped. Camel: filter(predicate).' },
  { front: 'Splitter', back: 'Turns one exchange into many (list items, file lines, JSON array elements). Camel: split(expression); add .streaming() for large payloads.' },
  { front: 'Aggregator', back: 'Collects related exchanges (correlation expression), merges them (AggregationStrategy), finishes on a completion condition (size/timeout).' },
  { front: 'Message Translator', back: 'Converts between formats. Camel: transform(), marshal()/unmarshal() for JSON/XML, or a bean for full control.' },
  { front: 'Dead Letter Channel', back: 'Error-handling EIP: retry with backoff, then park the failing message on a dedicated endpoint (DLQ) for inspection or replay.' },
  { front: 'Wire Tap', back: 'Copies a message to a secondary endpoint without disturbing the main flow. Camel: wireTap(uri).' },
  { front: 'simple language', back: "Camel's built-in expression language: ${body}, ${header.name}, ${body.total} > 100 — used in log/filter/choice/setHeader." },
  { front: 'direct:', back: 'In-memory, synchronous bridge between routes in the same service. The key to composing small, testable routes.' },
  { front: 'RouteBuilder', back: 'The class you extend to define routes in Java DSL. On Quarkus, any RouteBuilder CDI bean is discovered automatically.' },
  { front: 'Camel Quarkus extension', back: 'A Camel component packaged for Quarkus (camel-quarkus-*). Rule of thumb: one extension per endpoint scheme used in routes.' },
  { front: 'Dev Services', back: 'Quarkus feature: when dev/test config is missing, real infrastructure (Kafka, Postgres…) is auto-started in throwaway containers.' },
  { front: 'marshal / unmarshal', back: 'marshal(): POJO → wire format (e.g. JSON). unmarshal(): wire format → POJO. Needs a data-format extension like camel-quarkus-jackson.' },
  { front: 'onException()', back: 'Per-exception-type error policy overriding the default error handler — e.g. no retries for validation errors, big retry budget for transient DB errors.' },
  { front: 'handled(true)', back: 'Inside onException: swallows the exception so the caller receives your fallback response instead of an error.' },
  { front: 'AdviceWith', back: 'Test tool that rewrites a route by id: replaceFromWith() swaps the consumer, mockEndpoints() intercepts outputs, weaveByToUri() replaces steps.' },
  { front: 'MockEndpoint', back: 'Test endpoint with expectations: expectedMessageCount(), expectedBodiesReceived(), expectedHeaderReceived() … then assertIsSatisfied().' },
  { front: 'ProducerTemplate', back: 'Test/integration helper for injecting messages into a route: template.sendBody("direct:in", body).' },
  { front: 'fast-jar', back: "Quarkus's default packaging: a layout optimized for container layering and quick startup. Run with java -jar target/quarkus-app/quarkus-run.jar." },
  { front: 'Native image', back: 'GraalVM ahead-of-time compilation to a single binary: ~20–50ms startup, ~25–50MB RSS; slightly lower peak throughput than JVM mode.' },
  { front: '@RegisterForReflection', back: 'Quarkus annotation keeping a class reflectable in native images — needed for POJOs used by Jackson when native (un)marshalling fails.' },
  { front: 'quarkus.native.container-build', back: 'Build the native binary inside a container — no local GraalVM/Mandrel needed, only Docker or Podman.' },
];
